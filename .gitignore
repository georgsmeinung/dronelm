# Configuración unificada del proyecto (IPs, tokens, URLs de servidor)
# Copiar config/.env.example → config/.env y editar valores locales.
# config/.env

airsim-mcp/mcp_drone.log
airsim-settings/.tmp.driveupload
# Archivos de telemetría de vuelo de calibración
# callibration_flight/telemetry_*.csv

# Python bytecode & cache
**/__pycache__/
*.pyc
*.pyo
*.pyd

# Entornos virtuales
.venv/
venv/
venv.bak/

# Datos de vuelo generados por experiments/runner.py, batch_runner.py y main.py.
# Directorio único en la raíz: todos los subproyectos escriben aquí.
airsim-runs/
# Legacy: las rutas anteriores quedan ignoradas por si aún existen localmente.
airsim-loop/airsim-runs/
airsim-loop/runs/
airsim-plan/airsim-runs/

.claude/settings.local.json

